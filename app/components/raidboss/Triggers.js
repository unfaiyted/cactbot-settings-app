import * as React from 'react';

export default class Triggers extends React.Component<Props> {
  props: Props;

  render() {
    return (
      <div>
        Hello Triggers
      </div>
    );
  }
}
